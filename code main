#include "stdafx.h"
#include "iostream"
#include "math.h"
#include "fstream"
using namespace std;

int i, k, n;
double D = 0, Dk, Dn;


void main()
{
	if (cycle < 0)
		cout << "Error" << endl;
	else
	{
		Dk = sqrt(cycle(i, k, Dn, D, n));
		cout << "Answer = " << Dk << endl;
	}
}
